# Core pipeline dependencies. Install with:  uv pip install -r requirements.txt
# vLLM is optional and installed separately (see README) because it pins its own torch build.
torch>=2.3
transformers>=4.45
tokenizers>=0.19
accelerate>=0.30
sentence-transformers>=3.0
datasets>=2.20
huggingface_hub>=0.24
pyarrow>=15
pandas>=2.0
numpy>=1.26
pyyaml>=6.0
requests>=2.31
tqdm>=4.66
